# KNOWN GAPS & LEGACY — AI_ASSISTANT_CORE

## Incohérences corrigées par ce refresh
- README n’est plus laissé au niveau “backend minimal”
- `ROADMAP.md` racine n’est plus arrêtée au simple post-session 4
- `ARCHITECTURE.md` ne décrit plus le runtime comme un état flou ou purement host-side
- le projet est documenté avec les apports visuels de la session 4 et la canonisation runtime post-VM
- la surface `/execute` et la surface runtime restent alignées sur le récit documentaire
- le bridge Ollama réel en `12001` est désormais nommé explicitement

## Legacy confirmé
Les fichiers racine suivants restent des shims de compatibilité :
- `executor.py`
- `router_service.py`
- `task_classifier.py`
- `tool_selector.py`
- `task_routing.py`
- `comfyui_client.py`

## Gaps encore réels
1. le `portproxy` Ollama côté host reste encore transitoire dans sa forme, même s’il est canonique dans le setup actuel
2. pas encore de mode OpenAI-compatible public dédié au visuel
3. dette documentaire historique à ne pas réimporter via d’anciens fichiers
4. OpenWebUI acté comme UI opérateur optionnelle côté host, non canonique pour le runtime principal et non requise pour le fonctionnement cœur
5. le réalignement de `/debug/canonical` est déjà effectif côté code (`active_auxiliary_modules` expose explicitement les modules de support actifs) ; le reste du sujet est désormais purement documentaire
6. qualité finale des réponses encore améliorable sans refondre l’architecture
7. vigilance permanente contre la réintroduction de l’ancien bridge `12000` comme chemin runtime ; inventaire à date : toutes les occurrences restantes dans `core/docs/` sont soit des extrémités internes du `portproxy` (`12001 -> 127.0.0.1:12000`), soit des références historiques explicitement marquées comme non-runtime

## Nuance importante
“secondaire”, “auxiliaire” et “dormant” ne veulent pas dire la même chose.

Post-session 4, post-VM et post-hardening phase 1, le code classe déjà correctement :
- `visual_workflow_selector.py` et `visual_types.py` → `active_auxiliary_modules` (pipeline visuel)
- `tool_manager.py` → `active_auxiliary_modules` (lecture runtime)
- `planner.py` et `comfyui_launcher.py` → `dormant_modules`
- la VM participe réellement au design produit final
- le bridge Ollama actuel via `12001` participe réellement au runtime validé

Il n’y a plus de gap de surface sur `/debug/canonical` : le code distingue explicitement `active_runtime_modules`, `active_auxiliary_modules` et `dormant_modules`. Toute divergence résiduelle sur ce sujet est documentaire.
