# LIVRABLES — AI_ASSISTANT_CORE

## Livrable structurant déjà obtenu
Le projet a franchi le cap :
→ routeur minimal
vers
→ noyau d’orchestration avec plan explicite, exécution multi-stratégie et observabilité utile

## Ce que le snapshot valide réellement
- un classifier pondéré
- un routing central lisible
- une gestion simple des cas hybrides
- un planner step-by-step
- une surface `/execute` riche
- une synthèse web proprement isolée
- une génération visuelle via ComfyUI
- une couche d’intention visuelle légère mais utile
- une compatibilité OpenAI pour l’interface
- une surface runtime/debug exploitable
- un runtime principal désormais canonisé dans la VM

## Livrable spécifique de la session 4
- `subject_type / render_intent / style_flags`
- workflow selector visuel compatible V2
- prompt enrichment V2
- presets simples de format
- metadata `prepare_visual` enrichie
- support variantes / succès partiel
- contrat ComfyUI préservé

## Livrable spécifique de la phase post-VM
- backend lancé par `aicore-backend.service`
- SearXNG lancé par `aicore-searxng.service`
- bind local SearXNG resserré à `127.0.0.1:8081`
- checks `/health/runtime` réalignés sur le runtime host + VM
- validation reboot de la VM
- clarification opératoire host Windows / VM Ubuntu

## Ce que ce pack doit permettre
- relire vite l’état réel du projet
- éviter les fausses hypothèses de version
- éviter la confusion canonique / legacy / auxiliaire
- ne pas oublier les vrais gains de la session 4
- ne pas oublier la bascule post-VM du runtime
- guider les prochaines décisions d’architecture
- garder une mémoire claire entre sessions

## Prochaine valeur attendue
Pas un gros saut architectural.
La vraie valeur suivante est :
- alignement docs / runtime / API
- runbook post-VM canonique
- hardening léger du runtime réel
- amélioration contrôlée des prompts et outputs
- exposition plus propre côté UI des capacités déjà présentes
